{{ 'group.invitation.email.hello' | trans({ '%username%': username }) }}

{{ 'group.invitation.email.invited' | trans({ '%groupname%': groupname}) }}
{{ 'group.invitation.email.join_txt' | trans({ '%url%': url }) }}

{{ 'group.invitation.email.cheers' | trans }}
